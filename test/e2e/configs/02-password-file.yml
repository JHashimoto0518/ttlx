version: "1.0"

profiles:
  bastion:
    host: localhost
    port: 2222
    user: testuser
    prompt_marker: "$ "
    auth:
      type: password
      password_file: passwords.dat  # getpasswordを使用

routes:
  password-file-test:
    - profile: bastion
      commands:
        - whoami
        - hostname
