version: "1.0"

profiles:
  bastion:
    host: bastion.example.com
    user: user1
    prompt_marker: "$ "
    auth:
      type: password
      prompt: true

  target:
    host: 10.0.0.50
    user: user2
    prompt_marker: "$ "
    auth:
      type: password
      prompt: true
      password_prompt: "password:"  # 2段目以降で必須

route:
  - profile: bastion
  - profile: target
