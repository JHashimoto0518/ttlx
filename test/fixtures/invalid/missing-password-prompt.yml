version: "1.0"

profiles:
  bastion:
    host: bastion.example.com
    user: user1
    prompt_marker: "$ "
    auth:
      type: password
      prompt: true

  target:
    host: 10.0.0.50
    user: user2
    prompt_marker: "$ "
    auth:
      type: password
      prompt: true
      # password_prompt is missing - required for 2nd hop

routes:
  test-route:
    - profile: bastion
    - profile: target
