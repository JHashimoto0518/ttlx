version: "1.0"

profiles:
  bastion:
    host: bastion.example.com
    user: user1
    prompt: "$ "
    auth:
      type: password
      prompt: true

route:
  - profile: bastion
  - profile: nonexistent
