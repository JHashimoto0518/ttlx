version: "1.0"

profiles:
  bastion:
    host: bastion.example.com
    user: user1
    prompt_marker: "$ "
    auth:
      type: password
      prompt: true

routes:
  test-route:
    - profile: bastion
    - profile: nonexistent
