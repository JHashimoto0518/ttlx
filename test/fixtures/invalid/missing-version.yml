profiles:
  bastion:
    host: bastion.example.com
    user: user1
    prompt_marker: "$ "
    auth:
      type: password
      prompt: true
      password_prompt: "password:"

route:
  - profile: bastion
